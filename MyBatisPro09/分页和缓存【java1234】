Mybatis分页：
	1）逻辑分页
		数据库有100条数据，每页显示10条，将所有数据存入内存，再取出10条
	2）物理分页

Mybatis缓存:
	Mybatis默认情况下，MyBatis启用一级缓存，即同一个SqlSession接口对象调用了相同的select语句，直接会从缓存中返回结果，而不是再查询一次数据库
	开发者可以自己配置二级缓存，二级缓存是全局的
	
	默认情况下，select使用缓存的，insert，update，delete是不实用缓存的










































	
	
	
