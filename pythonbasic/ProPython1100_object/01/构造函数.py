"""
构造函数：
    __init__（）在使用类创建对象的时候自动调用
    注意：如果不显示的写出构造函数，默认会自动添加一个空的构造函数
"""
from Person2 import Person

per1 = Person("Jackson", 23, 175, 60)
per2 = Person("Tom", 20, 180, 56)

print(per1.name)
print(per2.name)
































