Set:
	它与字典相似，它是一组不存储值的键。

    Format:
        setName = {element1, element2, element3,,,elementN}

    Feature:
    	无序的，不重复的
		当它被创建时，它需要列表或元组或字典作为输入的集合。

    Operation of Set:
        .add(element)
            Insert the element into the Set.

        .update(element)
            Insert the element into the Set.

        .remove(element)
            Remove the element in the set.

        set1 & set2
            Intersection

        set1 | set2
            Union

    Transition of set
        ...















































































