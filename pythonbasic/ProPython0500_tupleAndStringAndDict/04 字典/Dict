Dict:
    Dict uses key-value storage,with extremely fast lookup speed.
    Key:
        1）每个Key都是唯一的，在字典中
        2）每个key都是不可变的对象
        3）key可以是字符串，是整数，但不能是列表

    Operation of Dict:
        .get(key)
            Get the value of the Dict according to the key.

        .pop(key)
            Delete the key-value in the Dict.

    Disadvantage:
        Take up a lot of memory.








































