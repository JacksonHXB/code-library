【字符串】:
    .split(str, [num])
        Split with characters in string.If there is a num, It will exist (num+1) elements.

    .splitlines()
        According to ("\r","\r\n","\n") ,split with specific characters.

    str.join(List)
		将列表中元素转为字符串

    max(),min()
		根据ASCII码的数值大小，获取该字符串中某个字符

    .replace(str1, str2, [num])
       	将字符串中的str1替换成str2，num表示替换的次数

    .maketrans(str1, str2)
    .translate(str)
		根据映射文件替换字符，.maketrans()设置映射

    .startswith(str, [start], [end])
		判断字符串以什么开头，后面两个表示范围

    .endswith(str, [start], [end])
		判断字符串以什么结尾，后面两个表示范围

    .encode(str, ["ignore"])
		对字符串进行编码

    .decode(str)
		对字符串进行解码

    .isalpha()
		判断字符串是否包含空格

    .isalnum()
		判断字符串是数字还是字符或字符串

    .isupper()
		判断字符串是否存在至少一个大写字母	

    .islower()
		判断字符串中是否至少有一个小写字母

    .istitle()
		判断字符串中每个单词的首字母是否大写

    .isdigit()
		判断字符串是否是数字

    .isnumeric()
    	如果字符串是由unicode编码，则判断字符串是否是数字

    .isdecimal()
		判断字符串是否是小数
		
    .isspace()
		判断字符串是否只包含空格






















































