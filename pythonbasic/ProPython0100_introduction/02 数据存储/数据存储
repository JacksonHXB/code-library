I,Memory:
    Memory is one of the most of important parts of a computer,and it is a bridge to communciate with the CPU.All programs in a computer run in memory.So the preformance of memory is very important to the computer.
	
II,Unit:
    1bit
    8bit == 1byte
    1024byte == 1K
    1024kb == 1M
    1024M == 1G
    1024G == 1T
    The data is in memory in binary form.

III,Binary System































































































































